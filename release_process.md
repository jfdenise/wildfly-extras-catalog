# Release a catalog for a WildFly version

* Create a local branch.
* Add the WildFly version as the last line of the `versions.txt` file.
* Git add/commit/push this change.
* Open PR against the main branch. The catalog is validated.
* When the PR is merged, the catalog is deployed.
